📝 CLI Todo App (Node.js)

A simple Command Line Interface (CLI) Todo Application built using Node.js and the built-in readline module.
This app allows users to add tasks, view tasks, and exit through an interactive terminal menu.